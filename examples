% [] is list and () is tuple

% \above[ \below] is a vertical list
% \left( \right) is a horizontal tuple list
% &c.

% $xs are typed cells or ranges, which could be expressed
% as functions that return true or false

% Mapping
\above[ \left( $key $value \right) \below]

% Tidy data table
\left[ \above( $header \above[ $value \below] \below) \right]

% Two-columns of mappings, with multiple tables per sheet
$pair = \left( $key $value \right)
$row = \left( $pair $pair \right)
$page = \above[ $row \below]
$sheet = \above[ $page \below]
